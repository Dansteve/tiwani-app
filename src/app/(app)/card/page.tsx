import { PlaceholderScreen } from "@/components/PlaceholderScreen";

// The Continuity Card (Product.md §4.6): generate, confirm contact (default off), share. The PDF
// and the opaque share link come from the api; no PII goes in the link. Foundation stub.

export default function CardPage() {
  return (
    <PlaceholderScreen
      title="Continuity Card"
      description="Generate a one-page support plan and share it. The card and its link come from the api."
    />
  );
}
