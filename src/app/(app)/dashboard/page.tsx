import { DashboardScreen } from "@/features/dashboard/DashboardScreen";

// The six-Life-Chapter dashboard (Product.md §4.3). The route segment renders the feature screen; the
// greeting, the chapter grid, the status mapping, and the empty state live in features/dashboard/.

export default function DashboardPage() {
  return <DashboardScreen />;
}
