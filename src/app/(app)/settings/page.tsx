import { PlaceholderScreen } from "@/components/PlaceholderScreen";

// Settings: profile, the care recipient's details (editable forever), preferences, sign-out.
// Foundation stub; wires to the api and Supabase Auth once those land.

export default function SettingsPage() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Your profile, the care recipient's details, and preferences. Editable any time."
    />
  );
}
